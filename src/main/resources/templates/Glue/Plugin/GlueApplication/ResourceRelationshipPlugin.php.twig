<?php

declare(strict_types = 1);

namespace {{ organization }}\Glue\{{ module }}\Plugin\GlueApplication;

use Spryker\Glue\GlueApplication\Rest\Request\Data\RestRequestInterface;
use Spryker\Glue\GlueApplicationExtension\Dependency\Plugin\ResourceRelationshipPluginInterface;
use Spryker\Glue\Kernel\AbstractPlugin;

/**
 * @method \{{ organization }}\Glue\{{ module }}\{{ module }}Factory getFactory()
 */
class {{ module }}ResourceRelationshipPlugin extends AbstractPlugin implements ResourceRelationshipPluginInterface
{
    /**
     * {@inheritdoc}
     * - Adds {{ module | dasherize }} resource as relationship by {{ relationshipParameter | camelCased | lcfirst }}.
     *
     * @api
     *
     * @param \Spryker\Glue\GlueApplication\Rest\JsonApi\RestResourceInterface[] $resources
     * @param \Spryker\Glue\GlueApplication\Rest\Request\Data\RestRequestInterface $restRequest
     *
     * @return void
     */
    public function addResourceRelationships(array $resources, RestRequestInterface $restRequest): void
    {
        $this->getFactory()
            ->create{{ expanderClassName }}()
            ->addResourceRelationships($resources, $restRequest);
    }

    /**
     * {@inheritdoc}
     *
     * @api
     *
     * @return string
     */
    public function getRelationshipResourceType(): string
    {
        return \{{ organization }}\Glue\{{ module }}\{{ module }}Config::RESOURCE_{{ module | dashToUnderscore | upper }};
    }
}
